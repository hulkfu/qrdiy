<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @code.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= @code.content %>
</p>

<%= image_tag @code.qr_png.url %>

<%= render 'qr_html', code: @code %>

<%= link_to 'Edit', edit_code_path(@code) %> |
<%= link_to 'Back', codes_path %>

<%= link_to 'Create Print', new_code_print_path(@code) %>
<%= link_to 'Prints', code_prints_path(@code) %>
