<% if statusable.respond_to?(:publishable_type) %>
  <%= render "publications/show/#{statusable.publishable_type.underscore}",
    publication: statusable %>
<% else %>
  <%# TODO: relation 的 view %>
  <%= statusable.try(:name) %>
<% end %>
