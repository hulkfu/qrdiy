<% if statusable.respond_to?(:content_type) %>
  <%= render "statuses/statusables/#{statusable.content_type.underscore}",
    contentable: statusable %>
<% else %>
  <%# TODO: relation 的 view %>
  <%= statusable.try(:name) %>
<% end %>
