<ul class="unstyled">
  <% projects.each do |project| %>
    <li>
      <%= link_to project.name, project_path(project) %>
      <span> - <%= project.description %></span>
    </li>
  <% end %>
</ul>
