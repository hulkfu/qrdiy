<%= simple_form_for(@project) do |f| %>
  <%= render "shared/error_messages", target: @project %>

  <div class="form-inputs">
    <%= f.input :title, label: "一个名字" %>
    <label>一些描述</label>
    <%= f.trix_editor :description, class: "trix-content" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
