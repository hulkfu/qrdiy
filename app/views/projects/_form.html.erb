<%= simple_form_for(@project) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title, label: "一个名字" %>
    <%= f.input :description, label: "一些描述" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
