<ul class="nav nav-pills">
<% Publication::REPLIES_TYPE_NAMES.each do |type, name| %>
  <li class="nav-item", data-publishable-type="<%= type %>">
    <%= link_to(name, change_reply_type_projects_path(type), remote: true, class: "nav-link #{type.eql?(:idea) ? 'active' : ''}") %>
  </li>
<% end %>
</ul>
